apply plugin: 'maven-publish'

tasks.register('sourcesJar', Jar) {
    from android.sourceSets.main.java.srcDirs
    classifier "sources"
}

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                groupId = "com.kk.taurus.playerbase"
                artifactId = 'txplayer'
                version = rootProject.ext.playerbaseTxVersion

                from components.release

                artifact(sourcesJar)

                pom {
                    name = "txplayer"
                    description = "Packaging txplayer by PlayerBase"
                }
            }
        }
    }
}